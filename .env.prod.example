# Copie para .env.prod e preencha TODOS os valores antes do deploy
# Nunca commite o .env.prod no git

# ── Banco de dados ────────────────────────────────────────────────────────────
MYSQL_ROOT_PASSWORD=troque_por_senha_forte
MYSQL_DATABASE=ccb
MYSQL_USER=ccb_user
MYSQL_PASSWORD=troque_por_senha_forte

# ── JWT ───────────────────────────────────────────────────────────────────────
# Gere com: openssl rand -base64 64
JWT_SECRET=troque_por_chave_aleatoria_longa
JWT_EXPIRES_IN=7d
